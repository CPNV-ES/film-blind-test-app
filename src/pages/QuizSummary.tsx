import { Link } from "react-router-dom";

const QuizSummary = () => {
    return (
        <div>
            <h1>À propos</h1>
    <Link to="/">Retour à l'accueil</Link>
    </div>
);
};

export default QuizSummary;
